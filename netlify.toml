[build]
  command = "npm install -g bun@latest && bun install --frozen-lockfile && bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PUBLIC_APP_URL = "https://esinsa-sga-app.up.railway.app"

[[redirects]]
  from = "/"
  to = "/landing/"
  status = 302
